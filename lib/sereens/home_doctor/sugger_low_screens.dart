import 'package:flutter/material.dart';

class SuggerLowScreens extends StatelessWidget {
  const SuggerLowScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
