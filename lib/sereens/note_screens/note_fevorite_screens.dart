import 'package:flutter/material.dart';

class NoteFevoriteScreens extends StatelessWidget {
  const NoteFevoriteScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.red ,
    );
  }
}
